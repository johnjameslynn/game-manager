class Conference {
    constructor (name, slug) {
        this.name = name;
        this.slug = slug;
    }
}

class Division {
    constructor (name, slug) {
        this.name = name;
        this.slug = slug;
    }
}